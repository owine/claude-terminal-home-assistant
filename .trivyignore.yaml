---
# Trivy vulnerability ignore list.
#
# Every entry below is a real, fixed-upstream CVE that this repo cannot remediate
# on its own — the fix lives in a binary we don't compile (a new HA base image for
# tempio, a new ha-cli release for ha) or behind a dependency cap we don't control.
# Review periodically and prune each entry when its stated condition is met.
#
# Entries are scoped with `paths` (matched against the scan target) or `purl` so a
# suppression can never leak to an unrelated component. NOTE: Trivy does not
# auto-discover this file — only the plain `.trivyignore` name is picked up by
# default — so both security.yml and publish.yml must pass it explicitly via the
# action's `trivyignores:` input.

vulnerabilities:
  # --- /usr/bin/tempio (Home Assistant base image templating binary) ---
  # Go stdlib + x/crypto CVEs in tempio, the templating binary baked into the
  # Home Assistant base image (built with Go 1.23.3 / x/crypto 0.26.0). Not
  # fixable here — tempio is not an apk package and is not recompiled by this
  # add-on; only a new HA base image release can resolve these. Re-scan and prune
  # when the base image is bumped. Last verified against base:3.24 / 2026.06.1
  # (2026-08-04) — tempio is still the 2024.11.2 build, unchanged since 2024-11-26.
  - id: CVE-2024-45337
    paths:
      - "usr/bin/tempio"
  - id: CVE-2025-22869
    paths:
      - "usr/bin/tempio"
  - id: CVE-2025-47913
    paths:
      - "usr/bin/tempio"
  - id: CVE-2025-61726
    paths:
      - "usr/bin/tempio"
  - id: CVE-2025-61729
    paths:
      - "usr/bin/tempio"
  - id: CVE-2025-68121
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-25679
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-27145
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-32280
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-32281
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-32283
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-33811
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-39820
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-39828
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-39829
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-39830
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-39831
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-39832
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-39835
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-39836
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-42499
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-42504
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-42508
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-46595
    paths:
      - "usr/bin/tempio"
  - id: CVE-2026-46597
    paths:
      - "usr/bin/tempio"

  # --- /usr/bin/ha (Home Assistant CLI) ---
  # golang.org/x/net v0.43.0 and x/text v0.28.0 CVEs compiled into the ha-cli
  # binary. We already pin HA_CLI_VERSION to the latest release (5.2.0); the fixes
  # (x/net >= 0.55.0, x/text >= 0.39.0) require an upstream ha-cli rebuild, so this
  # is not fixable by bumping our pin. Prune when a newer ha-cli release links
  # x/net >= 0.55.0 and x/text >= 0.39.0. Verified against ha-cli 5.2.0, still the
  # latest release (2026-08-04).
  - id: CVE-2026-25681
    paths:
      - "usr/bin/ha"
  - id: CVE-2026-27136
    paths:
      - "usr/bin/ha"
  - id: CVE-2026-39821
    paths:
      - "usr/bin/ha"
  - id: CVE-2026-56852
    paths:
      - "usr/bin/ha"

  # --- Both binaries ---
  # These land on tempio and ha alike, via different components, so they need both
  # paths: CVE-2026-33814 is Go stdlib in tempio but x/net in ha, and CVE-2026-39822
  # (os.Root symlink following, fixed in Go 1.25.12) is stdlib in both. Clearing
  # them needs an HA base image release *and* an ha-cli release.
  - id: CVE-2026-33814
    paths:
      - "usr/bin/tempio"
      - "usr/bin/ha"
  - id: CVE-2026-39822
    paths:
      - "usr/bin/tempio"
      - "usr/bin/ha"

  # --- cryptography (claude-terminal/ha-mcp) ---
  # CVE-2026-69247: Bleichenbacher oracle in cryptography's PKCS#7 EnvelopedData
  # decryption, fixed in 50.0.0. Not fixable here — ha-mcp caps the dependency at
  # `cryptography<50,>=48.0.0` (both the pinned 7.14.2 and the current 8.0.0), so
  # `uv lock --upgrade-package cryptography` resolves straight back to 49.0.0.
  # Not reachable either: cryptography reaches us via ha-mcp/authlib/joserfc, which
  # use JWS/JWE/JWT and X.509 — none of them call the PKCS#7 decrypt path, and the
  # add-on never decrypts attacker-supplied PKCS#7 blobs. Scoped by purl so it
  # covers both the uv.lock (fs scan) and site-packages (image scan) findings
  # without suppressing the CVE for any unrelated component.
  - id: CVE-2026-69247
    purl: "pkg:pypi/cryptography"
